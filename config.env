# OpenAI兼容API代理 for Z.ai GLM-4.5 环境变量配置
# 复制此文件为 .env.local 并根据需要修改

# ===== 必需配置 =====
# Z.ai 的访问令牌（非必需）
# 获取方式：登录 Z.ai 后从浏览器开发者工具中获取
ZAI_TOKEN=

# ===== 可选配置 =====
# 客户端 API 密钥（可选，默认: sk-your-key）
# 客户端调用时需要使用的 API Key
DEFAULT_KEY=sk-your-key

# 显示的模型名称（可选，默认: GLM-4.5）
# 客户端看到的模型名称
MODEL_NAME=GLM-4.5

# 服务监听端口（可选，默认: 9090）
# 如果设置为 9090，实际监听 :9090
PORT=9090

# 调试模式开关（可选，默认: true）
# 设置为 false 可减少日志输出
DEBUG_MODE=true

# 默认流式响应（可选，默认: true）
# 客户端未指定 stream 参数时的默认值
DEFAULT_STREAM=true

# ===== 高级配置 =====
# 上游 API 地址（可选，默认: https://chat.z.ai/api/chat/completions）
# 通常不需要修改
# UPSTREAM_URL=https://chat.z.ai/api/chat/completions

# ===== 使用说明 =====
# 1. 复制此文件为 .env.local
# 2. 修改 ZAI_TOKEN 为你的实际 Z.ai 令牌
# 3. 根据需要修改其他配置项
# 4. 重启服务使配置生效

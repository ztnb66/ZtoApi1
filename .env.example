# OpenAI兼容API代理 for Z.ai GLM-4.6 环境变量配置
# 复制此文件为 .env.local 并根据需要修改

# ===== 必需配置 =====
# Z.ai 的访问令牌（完全可选）
# 获取方式：登录 Z.ai 后从浏览器开发者工具中获取
# 注意：如果不设置此变量，系统会自动获取随机匿名token，无需手动配置即可使用
ZAI_TOKEN=your_zai_token_here

# ===== 可选配置 =====
# 客户端 API 密钥（可选，默认: sk-your-key）
# 客户端调用时需要使用的 API Key
DEFAULT_KEY=sk-your-key

# 显示的模型名称（可选，默认: GLM-4.6）
# 客户端看到的模型名称
MODEL_NAME=GLM-4.6

# 服务监听端口（可选，默认: 9090）
# 如果设置为 9090，实际监听 :9090
PORT=9090

# 调试模式开关（可选，默认: true）
# 设置为 false 可减少日志输出
DEBUG_MODE=true

# 默认流式响应（可选，默认: true）
# 客户端未指定 stream 参数时的默认值
DEFAULT_STREAM=true

# 思考功能开关（可选，默认: false）
# 控制模型是否展示思考过程
ENABLE_THINKING=false

# Dashboard功能开关（可选，默认: true）
# 控制是否启用监控面板
DASHBOARD_ENABLED=true

# ===== 高级配置 =====
# 上游 API 地址（可选，默认: https://chat.z.ai/api/chat/completions）
# 通常不需要修改
# UPSTREAM_URL=https://chat.z.ai/api/chat/completions

# ===== 使用说明 =====
# 1. 复制此文件为 .env.local
# 2. 可选：修改 ZAI_TOKEN 为你的实际 Z.ai 令牌（如不设置，系统将自动获取随机匿名token）
# 3. 根据需要修改其他配置项
# 4. 重启服务使配置生效

# ===== 示例配置 =====
# 生产环境示例：
# ZAI_TOKEN=your_actual_token_from_zai
# DEFAULT_KEY=sk-production-key-2024
# PORT=8080
# DEBUG_MODE=false
# DEFAULT_STREAM=true
# ENABLE_THINKING=true

# 开发环境示例：
# DEFAULT_KEY=sk-dev-key
# PORT=9090
# DEBUG_MODE=true
# DEFAULT_STREAM=false